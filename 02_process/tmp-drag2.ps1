$ErrorActionPreference = 'Stop'
opencli --profile 74u3g3us browser wt eval "(() => { const s = window.__dshWorktable && window.__dshWorktable.splitStore; const d = document.querySelector('.dsh-wt_splitDivider'); if (!s || !d) return 'no'; const before = s.chatW; const r = d.getBoundingClientRect(); const x = r.left + 2; const y = r.top + 300; const ev = (type, cx) => new PointerEvent(type, { bubbles: true, clientX: cx, clientY: y, pointerId: 7, button: 0, isPrimary: true }); d.dispatchEvent(ev('pointerdown', x)); d.dispatchEvent(ev('pointermove', x - 60)); d.dispatchEvent(ev('pointerup', x - 60)); return JSON.stringify({ before: before, after: s.chatW, dividerX: Math.round(r.left), viewportW: window.innerWidth }); })()"
